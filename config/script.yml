# Batch script options depending on batch type

default: &DEFAULT_VIEW
    name: vnc
    vnc?: true
    geom: 800x600
    dpi: 96
    idletimeout: 0
    extra_args: null
    otp?: false
    vncauth?: true
    pam?: false
    ssh_tunnel?: true
    tcp_server?: false

shared:
    <<: *DEFAULT_VIEW
    idletimeout: 300
    otp?: true
    vncauth?: false
    ssh_tunnel?: false
    tcp_server?: true
